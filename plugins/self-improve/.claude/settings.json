{
  "_comment": "これは ~/.claude/settings.json へ手動マージするためのサンプルです（コピー導入時のみ必要。プラグイン導入なら hooks.json により自動で有効化されます）。既存の settings.json を丸ごと上書きせず、hooks.SessionStart / hooks.SessionEnd の配列に下記オブジェクトを追記してください。フックはユーザーレベルに置くため、パスは $CLAUDE_PROJECT_DIR ではなく $HOME を使います。",
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$HOME\"/.claude/hooks/si-session-start.sh"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash \"$HOME\"/.claude/hooks/si-session-end.sh"
          }
        ]
      }
    ]
  }
}
